import React from 'react';
import { GasolineList } from '../components/GasolineList';

export const GasolineContainer: React.FC = () => {
  return <GasolineList />;
};

export default GasolineContainer;
