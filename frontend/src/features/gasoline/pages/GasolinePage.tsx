import React from 'react';
import { GasolineContainer } from '../containers/GasolineContainer';

export const GasolinePage: React.FC = () => {
  return <GasolineContainer />;
};

export default GasolinePage;
