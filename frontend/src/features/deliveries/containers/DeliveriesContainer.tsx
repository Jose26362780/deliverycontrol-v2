import React from 'react';
import { DeliveryList } from '../components/DeliveryList';

export const DeliveriesContainer: React.FC = () => {
  return <DeliveryList />;
};

export default DeliveriesContainer;
