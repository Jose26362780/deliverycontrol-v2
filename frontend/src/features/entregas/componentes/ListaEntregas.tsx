import React from 'react';
import { DeliveryList } from '../../deliveries/components/DeliveryList';

export const ListaEntregas: React.FC = () => {
  return <DeliveryList />;
};

export default ListaEntregas;
